   <div class="paddingTL">
        <div class="col-md-6">
          <div class="tile">
            <div>
              <h3 class="tile-title">Add List</h3>
              {{#if error_msg}}
                <div class="alert alert-danger">{{error_msg}}</div>
              {{/if}}
                 <p id="msgerror" style="color:red"></p>
            </div>
            
            <div class="tile-body">
              <form class="form-horizontal" method="POST" action="/createList">
               
                <div class="form-group row">
                  <label class="control-label col-md-3">List Name</label>
                  <div class="col-md-7">
                    <input class="form-control col-md-8" type="text"  placeholder="Enter Name Of item" value="" name="listname" id="listname" autocomplete="off" required>
                  </div>
                </div>
                <div class="form-group row">
                  <label class="control-label col-md-3">List Price</label>
                  <div class="col-md-7">
                      <input class="form-control col-md-8" type="text"  placeholder="Enter Price" value="" name="listprice" id="listprice" autocomplete="off" required>
                    
                  </div>
                </div>
               
                <div class="tile-footer">
                  <div class="row">
                    <div class="col-md-8 col-md-offset-3 col-xs-12">
                      <button class="btn btn-primary" type="submit"><i class="fa fa-fw fa-lg fa-check-circle"></i>Add</button>&nbsp;&nbsp;&nbsp;<a class="btn btn-secondary" href="/listItem"><i class="fa fa-fw fa-lg fa-check-circle"></i>Cancel</a>
                    </div>
                  </div>
                </div>
                <div class="tile-footer">
                  <div class="row">
                    <div class="col-md-8 col-md-offset-3">
                        {{#each message}}
                    {{message}}
                    {{/each}}
                    </div>
                  </div>
                </div>
              </form>
            </div>

          </div>
        </div>
        <div class="col-md-6">
          {{#if errors}}
              {{#each errors}}
                  <div class="alert alert-danger">{{msg}}</div>
              {{/each}}
          {{/if}}

        </div>
        {{!-- *****Javascript number validation******* --}}
       <script type="text/javascript">
       function isNumberKey(evt)
       {
          var charCode = (evt.which) ? evt.which : evt.keyCode;
          if (charCode != 46 && charCode > 31 
            && (charCode < 48 || charCode > 57))
             return false;
          return true;
       }
        function isMax()
       {
          debugger;
          let min =  document.getElementById("min").value;
          let max =  document.getElementById("max").value;
          if(parseInt(max) < parseInt(min))
          {
            document.getElementById("msgerror").innerHTML= "min price shuld be less than max price";
            return false;
          }
          else
          {
            document.getElementById("msgerror").innerHTML= "";
            return true;
          }

          
       }
        
       </script>

{{!-- javascript code for show tokenlist name with dropdown selection --}}
    
    </div>
    <script src="https://npmcdn.com/js-alert/dist/jsalert.min.js"></script>